#include "main.h"

/**
 * _abs - computes the absolute value of an input number
 *
 * @n: the unput number
 *
 * Return: the absolute value of n
 */
int _abs(int n)
{
	return (n >= 0 ? n : -1 * n);
}
